# Subscription definitions for fan-out routing.
#
# Each subscriber declares which tags it wants. When fairing processes an
# article, it checks the source's tags against each subscriber's tag list.
# If ANY tag matches, the article is written to that subscriber's output file.
#
# This is a file-based pub-sub model:
#   Publisher  = RSS source (tagged in sources.yaml)
#   Broker     = fairing (matches tags, writes to subscriber outputs)
#   Subscriber = downstream consumer (reads its own output file)
#
# Adding a new subscriber requires only a config entry here — no code changes.

subscribers:
  gp:
    # No active Group Portrait consumer remains. Keep the contract as history,
    # but do not grow the legacy JSONL until a consumer is explicitly restored.
    enabled: false
    tags:
      - database
      - ai
      - ai_research
      - ai_engineering
      - ai_infrastructure
      - ml_research
      - vector_db
      - software_engineering
      - architecture
      - philosophy
      - culture
      - history
      - psychology
      - anthropology
      - economics
      - business
      - buddhism
      - taoism
      - systems_thinking
      - mental_models
      - writing
      - sociology
      - management
      - data_platform
      - rationality
    output: /data/fairing/feeds/gp/store.jsonl

  ruoyi_talk:
    # The old ruoyi_talk store has no active reader. A future inbox must opt in
    # through a reviewed consumer contract instead of inheriting this fan-out.
    enabled: false
    tags:
      - database
      - ai
      - ai_research
      - ai_engineering
      - ai_infrastructure
      - ml_research
      - vector_db
      - software_engineering
      - architecture
      - engineering
      - data_platform
      - cloud
      - rationality
    output: /data/fairing/feeds/ruoyi_talk/store.jsonl
